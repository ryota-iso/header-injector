{
  "name": "@header-injector/extension",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp dev",
    "build": "vp build",
    "check": "vp check src shared package.json vite.config.ts index.html",
    "check:fix": "vp check --fix src shared package.json vite.config.ts index.html",
    "test": "vp test run --passWithNoTests --environment node"
  },
  "dependencies": {
    "@header-injector/core": "workspace:*",
    "@header-injector/ui-options": "workspace:*",
    "solid-js": "catalog:runtime"
  },
  "devDependencies": {
    "typescript": "catalog:build",
    "vite": "catalog:build",
    "vite-plugin-solid": "catalog:build",
    "vitest": "catalog:test"
  }
}
